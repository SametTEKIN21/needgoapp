/** Uygulama genel sabitleri. */

/** Web'deki gizlilik bildirimi sayfası (mağaza panellerine de bu URL girilir). */
export const GIZLILIK_URL = 'https://www.needgo.com.tr/gizlilik-bildirimi';

/** Ortak web sitesi kökü (moderasyon endpoint'i burada). */
export const WEB_KOK = 'https://www.needgo.com.tr';

/** İlan fotoğrafı otomatik moderasyon endpoint'i (web ile ortak). */
export const MODERASYON_API = `${WEB_KOK}/api/moderasyon`;
